{
  "name": "sass-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass -w style.scss -o ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^3.13.0",
    "normalize.css": "^5.0.0"
  }
}
